<%= form_with(model: [ @todo, @todo.comments.build ], remote: true, class: "comment-entry-form") do |form| %>
  <p>
    <%= form.text_field :body, :placeholder => "Add the comment. Hit 'Enter' to post", :class => "comment-entry" %>
  </p>
<% end %>
